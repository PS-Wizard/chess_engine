struct Move {
    from: u8,
    to: u8,
    peice: u8,
    captured: u8,
}

impl Move {
    // fn new(from: u8, to: u8) -> Self {
    //
    //     Move {
    //         from,
    //         to
    //     }
    // }
}
